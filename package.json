{
  "name": "elite-image-compressor",
  "version": "1.0.1",
  "description": "Takes a path as input and compresses all the images in that directory.",
  "main": "compress-images.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "compressor",
    "image",
    "compression",
    "files"
  ],
  "author": "jihoonj",
  "license": "ISC",
  "bin": {
    "compress-images": "compress-images.js"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "sharp": "^0.32.5"
  }
}
